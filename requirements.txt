# PipeCat core + integrations
pipecat-ai[deepgram,openai,silero,webrtc,sarvam]>=0.0.101

# Pipecat Flows (structured conversation flows)
pipecat-ai-flows

# Prebuilt WebRTC client UI (served at /client)
pipecat-ai-small-webrtc-prebuilt

# Web framework
fastapi>=0.115.6
uvicorn>=0.32.0

# Environment management
python-dotenv>=1.0.0

# Edge TTS (free, no API key)
edge-tts>=7.0.0

# Async support
aiohttp>=3.11.0

# Logging
loguru>=0.7.3
